name: mtg_companion_actions
description: Builds the MTGJSON-derived cards.sqlite bundle for the mtg-companion clients (web + mobile).
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  archive: ^4.0.0
  crypto: ^3.0.3
  drift: ^2.32.0
  http: ^1.2.0
  sqlite3: ^3.3.0

dev_dependencies:
  build_runner: ^2.4.0
  drift_dev: ^2.32.0
  lints: ^4.0.0
